Full-row square checkbox, lime when checked. Optional trailing `count`. Powers the subcategory lists inside the filter accordion.

```jsx
<Checkbox label="Video Editor" count={42} checked={on} onChange={setOn} />
```
