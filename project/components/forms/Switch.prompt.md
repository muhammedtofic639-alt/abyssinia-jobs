Lime pill toggle. Track turns lime + glows when on; knob slides with a subtle spring. Drives the Seeker⇄Employer view-mode flip and binary settings.

```jsx
<Switch checked={isEmployerView} onChange={setIsEmployerView} label="Employer view" />
```
