Round avatar; initials fallback when no `src`. `ring` lights a lime active ring; `verified` adds a blue ✓ dot.

```jsx
<Avatar src={photo} name="Selam Tesfaye" size={56} verified />
<Avatar name="AB" ring />
```
