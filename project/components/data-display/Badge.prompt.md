Status pill. Tones map to verification states: `pending`/`approved`/`rejected`, plus `accent`, `verify`, `neutral`. `solid` for filled, `dot` for a leading indicator.

```jsx
<Badge tone="pending" dot>Awaiting approval</Badge>
<Badge tone="approved" dot>Approved</Badge>
<Badge tone="accent" solid>NEW</Badge>
```
