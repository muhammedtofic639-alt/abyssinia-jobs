Rounded dark surface. `solid` default chrome, `glass` (blur) over photos/video, `elevated` for modals/sheets.

```jsx
<Card>Standard content</Card>
<Card variant="glass">Floats over the swipe photo</Card>
```
