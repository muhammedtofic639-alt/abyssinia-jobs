Circular icon-only button. `glass` floats over photos/video, `solid` sits on chrome, `accent` is the loud lime one. Always pass `label` for a11y.

```jsx
<IconButton variant="glass" label="Close">✕</IconButton>
<IconButton variant="accent" size="lg" label="Like"><HeartIcon/></IconButton>
```
